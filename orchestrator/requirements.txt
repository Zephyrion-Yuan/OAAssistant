# Python LangGraph orchestration layer for OAAssistant.
# Create a fresh mac venv (do NOT reuse the repo .venv, which is a Windows venv):
#   python3.12 -m venv orchestrator/.venv
#   orchestrator/.venv/bin/pip install -r orchestrator/requirements.txt

# --- Offline core (intake / schemas / store / executors / pure nodes) ---
pydantic>=2.7
openpyxl>=3.1
httpx>=0.27
python-dotenv>=1.0

# --- BFF gateway (front-end-facing HTTP API: proxy + profile + SSE chat) ---
fastapi>=0.110
uvicorn>=0.29

# --- LangGraph brain (graph / llm / run) ---
langgraph>=0.2.50
langgraph-checkpoint-sqlite>=2.0
langchain-core>=0.3
langchain-deepseek>=0.1
# Fallback provider if langchain-deepseek is unavailable (DeepSeek is OpenAI-compatible):
# langchain-openai>=0.2
